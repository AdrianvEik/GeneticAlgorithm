
== GeneticAlgorithm

This repository contains a C implementation of a configurable genetic algorithm.
The library is built with CMake and documented with Sphinx/Hawkmoth from the C
headers in `src/`.

